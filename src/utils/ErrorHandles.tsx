import { useRouteError } from "react-router-dom";

const ErrorHandles = () => {
  //   const myError = useRouteError();

  //   if (myError === 404) {
  //     return <div>This is loading 404 Error</div>;
  //   } else {
  //     return <div>This is Other Error</div>;
  //   }

  return <div>This is Other Error: </div>;
};

export default ErrorHandles;
